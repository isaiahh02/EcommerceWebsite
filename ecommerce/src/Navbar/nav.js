import React, { Component } from 'react';

class Navbar extends Component {
    render() {
        return (
            <h1>Home Shop Contact</h1>
        );
    }
};

export default Navbar;
// ReactDOM.render(<DemoCarousel />, document.querySelector('.demo-carousel'));
